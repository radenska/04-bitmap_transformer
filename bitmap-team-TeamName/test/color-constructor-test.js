'use strict';

const expect = require('chai').expect;
const fs = require('fs');
const colorCon = require(`${__dirname}/color-constructor.js`);
const fileReadHelper = require(`${__dirname}/../lib/bmp-file-helper.js`);

describe('Color Constructor Module', function() {
  describe('File output of each transform should be a buffer', function() {

  });
});
